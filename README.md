# Intrinsically motivated musical note exploration

## Gym environment

Install the Gym environment package:
```
python -m venv .env
source .env/bin/activate
pip install -e .
```

Run a random agent:
```
python random_agent.py
```
